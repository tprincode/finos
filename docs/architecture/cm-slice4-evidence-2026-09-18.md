# CM Slice 4 evidence — 2026-09-18

Files: `App.tsx` menu Working… / `elementDirty`; `CashElementsCatalog.tsx`; `CashYtd.tsx`; `cash_ytd.rs`; `CashElementListGet` `account:"all"`; chrome links on `CashManagement.tsx`. Register `ul` removed.
N1–N7: `cargo test -p golden-harness --test cash_slice4` (menu `aria-label="Menu working"`; `elementDirty` in leave/Restart; all-books Income three + Car + Health two + SSA two; catalog Edit → `CashElementEditor`; R2 series; `CashYtdGet` six books / five tax labels / remaining null ≠ 0; W1–W7 + G1–G6 stay).
Menu busy: visible Working… until the destination paints; fail uses the existing banner.
Elements catalog `aria-label="Elements catalog"` — one table; same editor as Week Ahead Edit / Register Add Element.
`CashYtdGet` `{ asOfDate, view: account|tax }`. Unknown remaining is — not $0. No MAGI $ column.
MAGI hook only: re-query `MagiProjectionGet` after IRA/Roth qualifying `CashDistributionPost` / `WeekAheadConfirm`.
W1–W7 and G1–G6 unchanged. No capture-grid rewrite, no Week Ahead command change, no Register running-cash math.
File → Restart Application so the rust host loads `CashYtdGet` / list-all.
Stop. Checking / transfers / household budget / MAGI screen not started.
