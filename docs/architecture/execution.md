# Execution board

Profile A product only. Local data SQLite is the system of record. Seed is a separate CLI, not an owner screen.

**Now:** Position Details extract — move the `screen === "position-details"` host into `features/position-details/`. Do not change owner behavior. Category/Alerts only if named.
**Next:** Home remainder, then Settings / Tickets / Calculator / Dashboard chrome.
**Ops (parallel):** none.
**Done this turn:** Trends bottom chart is grouped columns of paid dividends by calendar month for the two latest years. Dividend only. A month with no cash stays blank. Totals come from TrendsGet dividend points. Golden `trends_dividend_year_compare_is_grouped_month_bars`. File → Reload the Vite screen. Income Plan week + grid host moved to `features/income-plan/IncomePlanScreen.tsx` (week chooser math moved with it). App mounts the screen. Plan vs Decl stays on that screen. 2027 Confirm stays on System update tasks. Catalog `income-plan` is extracted. App.tsx is 12,570 lines / 482 KiB. Goldens `dividend_weeks_lives_on_income_plan_not_trends_middle` and `m3_weekly_updates_keeps_capture_and_week_ahead`. File → Reload the Vite screen. While Home or Income Plan is idle, background reads fill the Income Plan grid, dividend weeks, element list, and Tax Planning one at a time. Weekly grid opens from that memory when the date matches. Week Ahead, Register, Coverage, and Cash YTD stay on their own click. Golden `cash_slice4` n14. File → Reload the Vite screen. Screen packs / idle warm: `CashElementListGet` is read-only (no `ensure_horizon` / prune / seed). Element Management open runs `loadElementsPack` only. After Home or Income Plan paints, idle warm (1.2s) prefetches the element list + `TaxPlanningGet`; cancel on navigate or write. Horizon fill stays on Confirm / Register / Save. Goldens `cash_slice4` n13. Host Restart so rust list-get loads. 2026 1099 job received $14,625 is MAGI-included on Tax Planning (`1099 job` row) and Cash YTD tax type; `owner-1099-job-2026` magi_fact. Seed Form_1099 ROC stays off this year. Goldens `cash_menu_ia` tax planning + `cash_slice4` n6. Host Restart so rust TaxPlanningGet / MAGI load. Per-page activity is a live log card (`PageActivityCard`), not a progress bar. Finance client announces each read/write; the clicked control goes `is-unsaved` until that page is idle. First mount is Element Management. Income net/fed/state Element history YTD now lists seed IRA Friday parents (withholding slices) plus confirmed Saturdays — not only the 7 confirmed occs. Locked footer still $21,273.49 / $4,956.18 / $1,942.43 / $28,172.10. Golden `cash_slice4` n11. Host Restart so rust history loads. Element Management has a history register under a page-break header: pick any current or retired element, choose All / YTD / 6 / 3 / 1 month, and load every non-cancelled occurrence with account, date, debit/credit, status, activity, and duration totals. Query `CashElementHistoryGet`. Goldens `cash_slice4` n9/n10. Host Restart so rust history loads. HAKY lookback no longer false-fails on the inception anniversary. Monthly expected paid is completed month-ends after the launch month (current month only on that month-end). 21 Sep / 7 paid / inception 21 Jan is complete; 30 Sep unpaid vendor shell covers a one-period short. `declaration_lookback_short` auto-files when retrieve is ok. Goldens `haky_inception_anniversary_does_not_require_unpaid_current_month`, `haky_inception_anniversary_is_not_lookback_short`, `haky_month_end_unpaid_shell_is_not_lookback_short`. Host Restart so rust lookback loads. Car 2026 withdrawals locked as cash facts: 1/23 $600, 3/13 $650, 4/10 $850 (ledger, not $85), 5/15 $850, 6/12 $850, 8/10 $1,200 (posted `owner-car-wd-2026-08-10`), 9/8 $1,200 = $6,200. Tax type YTD dropped “Car withdrawal (not MAGI)”; Tax Planning annual/MAGI stays ROC (nontaxable) + ordinary + lot gains. Golden `car_withdrawal_ytd_is_cash_only_not_tax_type`. Host Restart so rust `TAX_ROWS` load. SSA 2026 YTD is posted facts: Tom 9×$2,865 = $25,785 (Jan–Sep; seed had two June Fridays) + Barbara 5×$1,331 = $6,655 (May–Sep) = $32,440. Live was $24,048 because Aug/Sep were never on `activity_event`. Posted `ssa-tom-2026-08/09` and `ssa-barbara-2026-08/09`. Barbara series start 2026-05-01; leftover 1st-of-month `tom`/`barbara` occs removed. Golden `ssa_ytd_is_tom_jan_sep_plus_barbara_may_sep`. Open Tax Planning / Cash YTD so the query reloads. Exceptions is two pages: Upcoming Transactions (highlight a planned hit) then Edit transaction (Cancel this transaction / Modify this transaction). `planned_occurrence_edit` leaves a cancelled stub on the original cadence date so horizon does not refill. Goldens `cash_slice4` n8 + `planned_occurrence_edit_moves_selected_upcoming_date`. Host Restart so rust/SQL `0044_planned_occurrence_cancelled` load. Element Management slice 4. Editor **N Exceptions** is a link to the Exceptions screen (`CashElementExceptions`). `PlannedOccurrenceSave` marks owner dates; series Save and leftover Saturday prune keep those rows and still drop unmarked dump dates. Goldens `cash_slice4` n4/n8 + `planned_occurrence_save_keeps_off_cadence_through_series_save`. Host Restart so rust/SQL `0043_planned_occurrence_exception` load. Cashflow Manager locked as a first-class Cash Management screen (catalog `cm-cashflow-manager` only; leftover “Cash register” module removed). Calendar + Home Trend + Manage Elements. YTD stays on Tax Planning. Goldens `cash_menu_ia` m2. Cashflow calendar cells no longer use a 6.4rem height; padding and line-height tightened, font size unchanged. File → Reload the Vite screen. Cashflow Manager dropped the Calendar/Trend lecture. Calendar | Trend sit on the same row as the account ticks. Trend is the Home `AccountCashFlow` (same account + duration selects, same weeks/asOf). Goldens `cash_register` r2 + `cash_slice4` n5. File → Reload the Vite screen. Cash YTD moved from Cashflow Manager to the bottom of Tax Planning. Calendar date click lists only that day’s transactions. Goldens `cash_menu_ia` m2 + `cash_register` + `cash_slice4` n5. File → Reload the Vite screen. Cash Management IA: Element Management is catalog-only. New menu **Cashflow Manager** (native `cash-cashflow` + in-app). Register uses the shared tick picker (`exactlyOne`, no All). Title is `{account} cashflow manager`. Month header is Planned Income / Starting Balance / Planned withdrawals / Ending balance. Goldens `cash_menu_ia` + `cash_register` + `cash_slice4`. File → Restart Application so the native menu item loads. Element Management slices 1–3. Shared `AccountTickPicker` (Elements All-or-one; Income Plan any combination). List is Deposits then Withdrawals; section + disabled on All; selecting a row opens the editor. Editor has account at the top, Expiration / Never expires, Exceptions count (old date table until slice 4). Goldens `n4_catalog_edit_opens_same_editor` + `income_plan_display`. Home/Register cash-flow header uses the title whitespace for Planned Income and Planned withdrawals from as-of through the selected duration (not from the month 1st). Axis still opens on the 1st. Health 12 months Planned Income agrees with Dividend Plan annual (monthly = annual ÷ 12). Golden `live_health_12m_planned_income_matches_dividend_plan`. File → Reload the Vite screen. Plan forecast get-well. Future Plan $ is qty × cadence periods × current plan on every Weekly/Monthly/Quarterly slot in the window (vendor day wins; missing vendor/assumed does not drop dollars). History is that week’s locked Plan vs Declared. Coverage Year forecast is next 12 months (live Income ~$41.1k vs Home $39,555.80, not 2025 $26,144). Coverage bottom is Plan vs Declared + % over/under. `PlanHistoryConfirm` moves unlocked weeks only. Goldens `plan_forecast` + `income_plan_week` + `cm_coverage`. Note `docs/architecture/plan-forecast-get-well-2026-09-20.md`. File → Restart Application so rust remaining-date holes load. Home/Register cash-flow window starts on the 1st of the as-of month; N months is as-of + N months (20 Sep 1m = 1 Sep–20 Oct), not last-of-calendar-month. Default 2m is 1 Sep–19 Nov, last Friday 20 Nov. File → Reload the Vite screen. Collectors is a separate module at `apps/desktop/src/features/collectors/` (`CollectorsScreen`, `CollectorEstablishScreen`, types, helpers). App mounts it; run/refresh/ticket commands stay on the host. Tools → Components lists Collectors as extracted. Golden `collectors_live_in_feature_module_not_app`. App.tsx is 12,986 lines / 489 KiB (Babel 500KB note gone). File → Reload the Vite screen. Home and CM Register Trend are one `AccountCashFlow`. Every book (Income, FI Roth, Car, Health, Speculation, Account 9) uses the same projector: last confirmed cash + planned dividends − planned debits. 2m last Friday is a dollar amount (`HOME_CASH_CHART_END_EXAMPLE` $850 on 2026-11-06). Each hit dot prints `Projected cash`. Golden `home_and_register_cash_chart_ends_in_amount`. File → Reload the Vite screen. Writer SOT second pass — first cut was table names, not typed cells. Weekly Updates grid is now one row per cell (six Totals, six Cashes, Account 9 ETF, 70% derived, week income is Plan $ not a writer, cash-gap reason). Note + canvas updated. File → Restart so rust S2/S4 assumed holes load. 2027 horizon S2+S4 shipped. `assumed_pay_date` + `PlanHorizonAssumeNextYear` / `PlanHorizonGet`. June include-next-year holes for every planned name (Confirm on Income Plan, `is-unsaved`). Chart/Register 1Y reads vendor dates ∪ confirmed assumed holes; does not invent next year. Vendor `issuer_pay_date` prunes the same assumed slot. Tax / remaining-year stay this 31 Dec. Goldens `register_1y_sees_assumed_2027_after_confirm`, `vendor_2027_prunes_assumed_hole`. File → Restart Application so rust/SQL load, then File → Reload the Vite screen. Amount / ROC Accept/Reject is for any symbol, not a named-ticker gate. 2027 horizon S1+S3 shipped. Collect **saves** vendor-printed / 8-K 2027 and still **drops invented** `derived_*` 2027. `remaining_year` tickets stay this 31 Dec. `remaining_pay_dates_for` cache is `(security, as_of)` so a 2026–2027 Register window plots stored 2027 vendor dates. Goldens `collector_saves_vendor_2027_without_remaining_year_ticket`, `recertify_mlp_keeps_published_2027_drops_invented`, `register_1y_sees_vendor_2027_after_2026_weeks`. Checklist R3 notes save ≠ ticket. File → Restart Application so rust collect/cache load. S2 June prompt + assumed holes and S4 prune not shipped. Review feedback accepted on 2027 dates — collect **saves** vendor-printed 2027; does **not** invent 2027 for completeness (June prompt + assumed holes); vendor 2027 prunes assumed; Decl $ ≠ Plan $; MAGI/Tax/YTD stay this 31 Dec. Plan `docs/architecture/plan-horizon-2027-plan-2026-09-20.md`. Canvases `plan-horizon-2027`, `review-feedback-2026-09-20`, writer SOT (two 2027 writers). Reader pass is the next design pass. No product code. 2027 Plan horizon external-audit pack only (no schedule / cash_ytd / AccountCashFlow / collector code). Note `docs/architecture/plan-horizon-2027-audit-2026-09-20.md`. Canvases `plan-horizon-2027` and refreshed `writer-source-of-truth` (proposed `assumed_pay_date` / `PlanHorizonAssumeNextYear`, not shipped). Owner cadence 1 June include-next-year; recommend approach 2a after review. Remaining-year / MAGI / Tax Planning stay this 31 Dec. Restart failed on sqlx `migration 39 was previously applied but has been modified` — 0039/0040/0041 were saved as CRLF after they ran as LF. Restored LF so checksums match the live DB. File → Restart Application. Writer source-of-truth audit (facts → SQLite table → command). Canvas `writer-source-of-truth`; note `docs/architecture/writer-source-of-truth-2026-09-20.md`. Readers not mapped yet. Home and Cash Management Register Trend are one `AccountCashFlow` (`features/cash/AccountCashFlow.tsx`). Home and CM Register import that same component; `HomeAccountTrendFocus.tsx` is a re-export only. Default book is **Health** (2 months). File → Reload the Vite screen. Home Account cash flow projection no longer waits on deferred `TrendsGet`. `HomeOpenGet` already built the week cash series; it now returns `accountValue.weeks` and Home paints from that. Cash-hit dots use `CashRegisterGet` `hitsOnly` (no horizon writes, no running-cash series) and start only after those weeks exist so they do not fight Home open on the SQLite pool. File → Restart Application so rust weeks load, then File → Reload the Vite screen. Tax Planning household table includes **HSA withdrawals** from the Health book (no MAGI, same YTD/projected as Cash YTD). Every Register book is now on that table: IRA (Income + Speculation + Account 9), Roth, HSA, Car via ROC/ordinary/gains, SSA. File → Restart Application so `TaxPlanningGet` loads the HSA row. Initial-load harden/speed P0–P2: migration `0041_open_path_indexes` (ledger date/account indexes + occurrence/price unique after dedupe + activity scale/type triggers); Home open uses `HomeOpenGet` (one `account_value_home_view`) and defers Income Plan grid / `DividendPerformanceGet` all / CM-tax tail until those screens open; `TaxPlanningGet` embeds Car + YTD; range list APIs + request-scoped Income Plan week/declaration cache; SQLite 64 MiB cache + 256 MiB mmap, `busy_timeout` 15s, pool stays 4; Save data snapshot is `VACUUM INTO`; last-price persist refreshes Home only. Goldens `open_path`, `open_path_indexes_and_pragmas`. File → Restart Application so rust/SQL load, then File → Reload the Vite screen. Cash Management menu **Tax Planning** (was Car Account Tax Planning). Car table unchanged. New household YTD/Projected/Total by tax type (IRA = Income + Speculation + Account 9; Roth; ROC; ordinary; SSA; LT/ST gains) plus MAGI threshold from `MagiProjectionGet` (not a second invented MAGI). File → Restart Application so the native label and `TaxPlanningGet` load. Coverage Week/Month/Year chips show `Loading Month…` (same for Week/Year) and hide stale tables until `CashCoverageGet` returns. File → Save data snapshot confirm is a centered popup (`home-av-dialog`), not the bottom unsaved bar. File → Reload the Vite screen. File → Save data snapshot also copies `local.sqlite` (and `-wal`/`-shm` if present) into the same dated folder as the workbooks: `raw-data/<today>/` under the live app-data directory. File → Restart Application so rust copy loads. Coverage title is Weekly / Monthly / Annual comparison. Income vs expenses is the average of the same lookback (13 complete weeks / 12 months / last year) and Δ. Monthly Car/Health convert into the week (amount × 3/13) so they show on Weekly. Plan vs Actual is average plan income vs average recorded income on that same lookback. File → Restart Application so rust conversion loads, then File → Reload the Vite screen. Coverage is two like-for-like tables for the same Week/Month/Year: Plan income vs Plan expenses (margin + average of those plan margins), then Plan vs Actual with Δ. Actual/Δ stay — until a paid dividend in that same period. File → Restart Application so rust row fields load, then File → Reload the Vite screen. Coverage table caption is this Sat–Fri week (W38 · Saturday–Friday). Average / Plan / Actual headers are right-aligned over the dollars and name the window (last 13 weeks vs this week). Actual stays — until a paid dividend or confirmed Element in this period. File → Reload the Vite screen. Cash Management Coverage is the fourth child (in-app + native `cash-coverage`). One row per book (Income, FI Roth, Car, Health, Account 9) plus Total. Lead Average (completed periods; blank ≠ $0), then this-period Plan and Actual. Income Plan minus Elements; SSA and typed household budget stay out. Query `CashCoverageGet`. File → Restart Application so the native item and rust query load. Home Income 1 month Ending cash is no longer unknown when the last snapshot is Saturday-keyed or sits on the week that holds month-end (1m Sep 1–30 includes Fri 2 Oct) or only exists before September (that Friday opens the month; Register hits still fold forward; unknown ≠ $0). File → Restart Application so rust Friday cash join loads, then File → Reload the Vite screen. Home cash-flow dots are one green and one red per Sat–Fri week (not per calendar day). Hover lists each hit with its date and a Total. File → Reload the Vite screen. Home/Register drop that week's Income Plan row when a paid actual exists for the name (Actual replaces Plan; future weeks stay Plan). Dividend actuals keep the symbol. Elements already flip on Confirm. File → Restart Application. Saving Income State (or any series) on Saturday now updates this week's occurrence, the Elements Next amount, Week Ahead, and Register — they read the saved series amount, not a frozen same-day leftover. File → Restart Application. Home ending cash and the cash line now fold Register dividend/element hits after the last saved Friday snapshot (unknown start stays unknown). Hover amounts and the balance are the same running cash. CashRegisterGet on this chart times out at 30s. File → Reload the Vite screen. Save data snapshot is a full backup of stored facts (not a seed-reload question). Adds `Template_PlanHistory.xlsx`, `Template_PaymentPatterns.xlsx`, `Template_PayDateOverrides.xlsx`, `Template_RocObservations.xlsx`, `Template_RetrieveRuns.xlsx`, `Template_WorkTickets.xlsx`, `Template_Exceptions.xlsx`, `Template_HoldingQtyEvents.xlsx`, `Template_LotAssignments.xlsx`, `Template_TaxProfiles.xlsx`, `Template_MarketValueDaily.xlsx`, `Template_DividendActuals.xlsx` on top of the prior workbooks. File → Restart Application then File → Save data snapshot. Home cash-flow dots already combine like color per calendar day (one green deposits, one red withdrawals); hover lists every name and amount. File → Reload the Vite screen for dots. Home Account cash flow projection is one tool for every cash book (Income, FI Roth, Car, Health, Speculation, Account 9). Axis is calendar time from the 1st of this month forward. Friday seed/snapshot cash is the line (unknown ≠ $0). Payable-day planned dividends plot by symbol; recurring elements and posted dividend actuals plot on their occurred-on day. Same Income rules on every Register book. File → Restart Application so rust week cash fields and per-symbol Income Plan rows load. Home Account cash flow projection always starts on the 1st of the current month and looks forward (default 2 months = 1 Sep–31 Oct). History stays off. Axis is every Sat–Fri week in that window; seed cash sits on Friday; Saturday net/fed/state map into that week. File → Restart Application so rust `periodStart`/`periodEnd` load. Three weekly Income withdrawals (net/fed/state) plot as red dots via `CashRegisterGet` `includeUnconfirmedPast`. File → Restart Application (rust flag) then Reload Vite. Home Income cash-flow chart reads `incomeCashMinor` (Income Cash Balance), not `incomeBalanceMinor` (Income Total Balance / ~$209k account value). File → Reload the Vite screen. Home Account cash flow projection header is Ending cash (last known week in the window, unknown ≠ $0). Dashed linear-trend line removed. Hoverable element dots from CashRegisterGet (name + amount). File → Reload the Vite screen. Home cash-position chart title is Account cash flow projection (`clamp` so it fits the right column). File → Reload the Vite screen. Home right column under Dividend Plan is one Trends cash-position chart (`HomeAccountTrendFocus`) — Income/Roth/Car/Health/Speculation week balances, not market value. Title account + duration dropdowns. Default Income + 2 months. File → Reload the Vite screen. Auto last-price and auto collectors share weekday 9–4 Eastern (`in_auto_schedule` / `LastPriceAutoWindowGet.inSchedule`). Last price keeps the 4-hour retrieve_run cooldown (`allowed`). Collectors run once per local date only (`declarationRefreshedOn` already today). Weekend/outside-hours skip both; a 4-hour last-price skip does not skip collectors. Refresh last prices / Refresh declarations stay force. Goldens `collectors_share_clock_not_four_hour`, `position_details`, `accessibility`. File → Restart Application so rust host picks up `inSchedule`. Trends graphs include every saved week (newest Friday even if the week is still open) and open on All data. WeekCaptureAccept remounts Trends and re-queries `TrendsGet` for that `periodEnd`. Golden in `account_value.rs`. File → Reload the Vite screen. Element editor shows Working… and paints every button orange while dirty or in flight (`is-owner-edit-active`; `is-unsaved` stays orange when disabled). Delete/open set `busy`. One Restart covers this plus the last rust Week Ahead fix. Week Ahead now uses the same Element series as the CM catalog (no Saturday dump of monthly Car/Health/SSA; leftover `hsa1` $200 is gone). Catalog Next includes today so this week's Income matches. Golden `week_ahead_follows_element_series`. File → Restart Application so rust host picks up `week_ahead.rs`. Weekly Accept no longer dies on undefined `weekIncome` (`buildBody` → `WeekCaptureAccept`). Restored `suggestedMonthlyDivsMinor`. Disabled Accept now prints why (totals / closed / cash-gap reason). Golden in `accessibility.rs`. File → Reload the Vite screen. Elements/Register no longer keep `Loading Cash Management…` under the grid when `week` is null. That line is only for Weekly when the week pane is missing. Golden `elements_desk_does_not_paint_false_week_loading`. File → Reload the Vite screen. Car tax table prints `ytdRocUnknownReason` when YTD or Planned ROC is null — never `$0.00` from `?? 0`. Footer totals stay unknown when ROC is unknown. Goldens `m5_unknown_ytd_roc_is_null_plus_reason` + cash_management needles. File → Reload the Vite screen. External audit zip of the current working tree: `Documents\Financial\finos-external-audit-2026-09-18.zip` (copy also at repo root). SHA256 `e092a57e8c315c294b01418ad149026fba737401bb13573dded9b6de5b71e015`. Excludes node_modules, target, .git, .cursor, updater-keys, live DB, personal statement PDFs, and prior review zips. Owner `health.xlsx` weekly Health cash (Saturday columns; 10/3–9/19 blank) written to Health `account_balance_snapshot.cash_minor` on the prior Friday. Empty cells skipped, not $0. Account totals unchanged. File → Restart Application so Register reads the new week cash. Owner `car.xlsx` weekly Car cash (40 Saturday columns, 2026-01-03 through 2026-10-03) written to Car `account_balance_snapshot.cash_minor` on the prior Friday (Sat→Fri). 37 weeks updated; 9/18, 9/25, and 10/2 skipped until those week snaps exist. Account totals unchanged. File → Restart Application so Register reads the new week cash. Car running cash uses only a week `cash_minor` before the window — leftover SPAXX is never a start. Past unconfirmed $850 firsts do not fold. Trend series opens on `periodStart` (month 1st) at that verified cash. Owner Car facts: 2026-08-28 cash $3,042.51 and 2026-09-08 withdrawal $1,200. Golden `car_september_opens_on_first_with_owner_cash`. File → Restart Application so rust host picks up `cash_register.rs`, then File → Reload the Vite screen. Car Register no longer uses today’s leftover SPAXX as a July opening. Running cash stays — unless a week `cash_minor` before the window exists, or a cash-par lot was already open on the period start. August leftover is not July cash. Golden `later_cash_lot_does_not_invent_july_running`. File → Restart Application so rust host picks up `cash_register.rs`. Register 3M/1Y (and other period chips) show `Loading {period}…` then replace the Trend series (`CashRegisterGet` for that window). Title includes the period. File → Reload the Vite screen. Register Trend is `{book} running cash` (Income default) with a date axis, `$ Total` axis, hover amount, and hover/click that focuses that day’s Register rows. Calendar day click does the same. File → Reload the Vite screen. Register calendar stays a one-month grid with Previous/Next, keeps showing on 2W/3M/1Y, names the book (`Income Register` default), and prints credits dark-green bold / debits bold red. Pressed account/period/view chips are navy. File → Reload the Vite screen. Register 1M Calendar is a Sun–Sat month grid (day number, day net, running cash) from `register.series`; Trend is the same series with past solid / future dashed. Slice 3 lock was “month grid,” not a date list. File → Reload the Vite screen (CSS/TSX). Element catalog Next date / Next amount follow the saved series for every element, not leftover occurrence rows. `CashElementSave` ignores editor-listed off-schedule dates and true-ups amounts to the series. `CashElementListGet` prunes the same leftovers on open (MyClearbalance 10/01 $200 → 10/15 $203). Goldens `save_with_stale_editor_occurrences_moves_next`, `list_get_prunes_leftover_off_schedule_without_save`. File → Restart Application so rust host picks up `cash_register.rs`. Car Account Tax Planning is a table: YTD / Planned / Total YTD + Planned × Ordinary, ROC, Long Term Capital Gains, Short Term Capital Gains, plus a Total row. Missing activity prints $0, not unknown. WPAY is the former listed ticker for TOPW. `current_listed_symbol` maps WPAY→TOPW; Car YTD ROC uses TOPW’s estimate on leftover WPAY dividend rows (not mixed/unclassified). Golden `wpay_car_dividends_use_topw_roc_estimate`. File → Restart Application so rust host picks up `queries.rs`. Owner ROC Accept this month: three `roc_pct_change` tickets (SVOL 25→80.60, TSPY 85→100, QYLD 80→99.70) on 14 Sep; TOPW was already 100% with no change ticket. CM menu IA — Cash Management is a top-level menu with Element Management, Weekly Updates, and Car Account Tax Planning. Plan shortcut opens Weekly Updates. Visible Car heading is Car Account Tax Planning; unknown YTD ROC prints a reason (never $0). Evidence `docs/architecture/cm-menu-ia-evidence-2026-09-18.md`. File → Restart Application so the native menu and rust `ytdRocUnknownReason` field load. Checking / transfers / household budget / MAGI screen / phone clone not started. CM Slice 4 nav / Elements catalog / YTD shipped. Menu Working… (`aria-label="Menu working"`); `elementDirty` joins leaveWithoutSaving and Restart. One Elements catalog (`CashElementListGet` `account:"all"`); Register `ul` removed; same `CashElementEditor`. CM chrome links Elements / Register / YTD. `CashYtdGet` Account|Tax type (remaining null ≠ $0). MAGI hook re-queries `MagiProjectionGet` after IRA/Roth posts. N1–N7 in `crates/golden-harness/tests/cash_slice4.rs`. Evidence `docs/architecture/cm-slice4-evidence-2026-09-18.md`. File → Restart Application so rust host picks up `CashYtdGet`. Checking / transfers / household budget / MAGI screen not started. Coding start is one job: repo-root `finos.bat`. Removed `start-finos-supervisor.bat`; host Restart starts `finos.bat` if the parent is missing. Slice 3 Register + Elements shipped (`CashRegisterGet`, `CashElementListGet` / Save / Delete, `PlannedOccurrenceDelete`; `CashRegister.tsx` + `CashElementEditor.tsx`; Week Ahead Edit opens the editor). Evidence `docs/architecture/cm-slice3-evidence-2026-09-18.md`. R1–R8 in `crates/golden-harness/tests/cash_register.rs`. File → Restart Application so rust host picks up the new query/command arms. Slice 4 not started. ORC collector is ok today (`last_run_ok`, 2026-09-18 07:49 unchanged). The open `declaration_retrieve_miss` from 2026-09-15 stayed because DeclarationRefresh/fleet `unchanged` stamped last_run without `work_ticket_auto_file_declaration`. Retry only bumped `last_seen`. Wired auto-file on declaration ok persist + SyncMisses when latest run is ok. Golden `unchanged_ok_auto_files_open_retrieve_miss`. Live ORC ticket auto-resolved. W37 no longer lists GLAD from the leftover-actual append when payable is 2026-09-30 (W39). Income Plan Plan $ for W37 is calendar/decl names only ($922.85 live; GLAD $47.59 stays on W39). Cash week desk uses that same Plan $ for the Income Plan week; capture Planned weekly income no longer falls back to Decl/actuals. Goldens: `off_calendar_actual_does_not_move_plan_week` (GLAD absent W37; Trends W37 Planned $ = Income Plan Plan $), `leftover_record_pay_date_plans_on_payable_week_not_record_week`. File → Restart Application so rust host picks up `queries.rs`. Goldens now lock the CLM/CRF/GLAD miss: `leftover_record_pay_date_plans_on_payable_week_not_record_week` (W39 payable, not W37 record; Trends Planned $ = Income Plan Plan $), `off_calendar_actual_does_not_move_plan_week`, `collector_retrieve_moves_leftover_record_after_date_passed`. Checklist R3/R10 + `planned-week-authority` in core-functions.json. CLM/CRF/GLAD leftover record dates no longer stay as the live pay date after the vendor payable is known: merge moves the earlier same-month leftover even when that leftover day has passed and the payable is already stored; `paid_payable_supersede` is only for a real paid month pulled earlier. Cornerstone collect follows the press-releases hub (May and Aug quarterly PDFs) and keeps recordDate so leftover-ex can move. Gladstone probe prefers the newsroom payable over the JS dividend-history widget. Live calendar is now payable 2026-09-30 (W39) for CLM/CRF/GLAD; Oct–Dec stay on the announced month-end. Cash Management / Trends **Planned weekly income** is Income Plan Plan $ for that Sat–Fri week (`plan_history` × remaining qty), not Decl $. A name listed only because of an off-calendar broker actual does not put Plan $ on that week. Slice 2 Week Ahead lists unconfirmed Sat–Fri planned Element occurrences (SSA lookback four calendar days) and Confirm posts through existing writers (`WeekAheadConfirm` → Income `IRA_Distribution`, SSA `SsaConfirm`, Car `Withdrawal`, Health `HSA_Withdrawal`). Tables `cash_element` / `planned_occurrence`. Capture grid G1–G7 and Planned/Reported stay. W1–W7 green. Register not started. Cash week desk, capture review, and Trends charts keep Planned vs Reported. Home and Trends graphing period default is 6 months (`DEFAULT_GRAPH_PERIOD`). Last-price charts now mark remaining qty on the event date (not this week’s lot edit). SOXL: 40 until 2025-12-15 (16 sold), 24 until 2026-04-10, then 14. MSTU: 200 until 2025-12-03 reverse split, 20 until 2026-08-24, then 2. Lot remaining is unchanged. Blank Home was Vite `formatPerShare` declared twice in `packages/ui-components/src/index.tsx` — kept the exported five-decimal helper; Income Plan Decl $ uses `formatDeclPerShare`. File → Restart cannot run from a blank webview (in-app File menu never loaded). Vite on 1420 now serves the fixed file. Declaration GET waits 20s on the fleet pass, then retries the stored Template Dividend at 45s and 90s (ticket Retry does all three). Timeout is not Recreate. ORC diagnose: stored Template Dividend is `https://ir.orchidislandcapital.com/stock-information/dividends-splits`; owner URL is the `www` twin of the same page. Collect said empty while the GET has a Payable table — adapter now retries via Windows curl, treats `ir.`/`www` as one host, and miss payloads keep GET evidence (`htmlLen` / payable / td). Recreate adapter keeps stored Template Dividend / ROC URLs, validates with `CollectorRecertify`, and may retrieve with that URL. It does not re-seed or re-import successful pays / Plan / identity; Confirm Plan stays off when Plan is unchanged. Recreate / Add Position per-share now uses `formatPerShare` (always five decimals: 0.13000). Confirm Plan reports stored vs proposed Plan and writes Plan / share only (`PlanHistoryConfirm`) — button reads Update Plan / share when lots exist, not “all data.” Recertify still does not wipe stored pays; same stored amounts do not auto-skip the sheet. Owner sheet W34–W36 written through `WeekCaptureAccept` (W36 Profit $1,053.00). W29–W33 left alone. Coding `start-finos-dev.bat` no longer does `set "ERR=%ERRORLEVEL%"` / `if not "%ERR%"=="0"` — cmd was executing `'"ERR=0"'` after Vite died (npm 4294967295 = killed process). Exit is now `DEVEXIT` + `if %DEVEXIT% EQU 0` / `EQU 4294967295`. Killed-Vite is the old session ending, not a start failure. Weekly capture cash may be left blank on input and Accept: that account skips the week’s cash update (not $0, no Adjust). Totals still required. Slice 1b capture grid: week dropdown then one table with Income, FI Roth, Speculation, Health, Car, Account 9 (Total + Cash). Account 9 types ETF total; 70% is read-only × 0.70 (10000 → 7000; blank is — not $0). Accept on that screen via `WeekCaptureAccept` (PR #10 recon kept; no Adjust when cash matches). Edit refills the same table. No Next between accounts. DeclarationRefresh on open runs once per local date (`declarationRefreshedOn` == `declarationAsOf`); Retrieve declarations still force-runs. File → Restart that showed Vite ready then `beforeDevCommand` / `Lifecycle script \`dev\` failed` was a second `start-finos-dev.bat` killing port 1420. Coding launch now takes `%LOCALAPPDATA%\com.finos.desktop\dev-start.lock` with a live cmd PID (`WriteAllText`); a second start exits without `taskkill` on Vite. Supervisor waits for the exe and 1420 to go free, then Start-Process-es one new console. The old window’s `dev` failed line is that Vite exiting — use the new `finos (dev)` window. File → Restart was also exiting with no relaunch: leftover restart.token, a Sept 11 supervisor.lock, and no supervisor window. Host now waits for `cmd /c start` of start-finos-supervisor.bat (breakaway console) and the lock is a live PID, not a window title. Last price auto: weekday 9–4 ET plus 4-hour cooldown on any price retrieve_run (ok, fail, or started). LastPriceRefresh writes retrieve_run at start so File → Restart inside 4 hours skips. LastPriceAutoWindowGet includes that cooldown so the busy bar never starts on skip. Manual Refresh still `{ force: true }`. Yahoo last-price HTTP is 3s per symbol; miss keeps the stored price, never $0, and the fleet continues. Home Schwab Total now plots typed week Fridays (dashed, connected) and live qty × last price; incomplete cash-only history is not the account total. Establish recertify is a product command (`CollectorRecertify`): same `collector_is_complete` E-gate after first `LotOpen` and after Research on a name that already has lots (Recreate). Lots do not skip it. First-lot `collector_incomplete` and extra-lot grandfather stay. Fail raises `collector_establish_incomplete` (Recreate adapter tool); pass files that ticket. First ten InScope collectors are official complete **10/10** after owner Accept ROC of the stored plan % (`RocPlanConfirm`, same body as Collectors Accept ROC; last_run_ok unchanged): QYLD 99.7, SVOL 80.6, TSPY / HAKY / QDTE / RDTE / TOPW / XDTE 100. AMDW and QDVO were already locked. Ticket Accept on `roc_pct_change` now also sets `needs_roc_research=false` so E10 does not stay open after Accept. Quality gate R9: after the 40 run, fail count (not current today) must equal open retrieve-failure tickets on those names (`declaration_fail_ticket_parity` on DataSummary / CollectorStats / WorkTicketSyncMisses). Home and Collectors print Fail N / miss tickets N; mismatch is “gate broken.” TOPW Monday declaration was on the vendor page (`2026-09-16` $0.219827) while stored newest was `2026-09-10` — not an adapter miss. Today’s Home runs were R5 unchanged skips after the 07:07 stamp; locked weekday now re-fetches. Home **39 of 40** was EFC: today's declaration retrieve is `last_run_ok=false` (`Issuer page empty`) but `WorkTicketSyncMisses` skipped reopening because a Sep 7 `declaration_retrieve_miss` was `owner_filed` (note from `collector_miss_loop`: `auto: latest declaration retrieve is ok`). That skip broke R2 — a later miss must ticket again; Home still counts the failed today. Removed the owner-filed suppress. After File → Restart, Home refresh reopens the EFC miss ticket. Ticket Accept/Reject (and amount Except/Reject) use `is-unsaved` on the clicked button from click until `WorkTicketResolve` + refresh finish; a second click is ignored while pending. First five 19a-1 collectors were **not** complete until this turn: QYLD `--probe` had been reading `QYLD_Form-19a_07212025.pdf` (July 2025, 100%) because Form-19a stamps were string-sorted and `.docx` seeds were treated as hubs. Fixed: `MMDDYYYY` sorts as (year, month, day), current-year files only when any exist, `.docx` queued as notices, ZIP central-directory extract, Windows curl fallback on read timeout. Live QYLD is now August 2026 `QYLD_Form-19a_08242026.docx` **99.72%** (ticket already open; plan stays 80%). AMDW `--probe` was empty because `fund-filings#amdw` is not a notice URL and search ran before the issuer page; `live_roundhill_roc` now reads the ETF page first — live **100%** matches plan. QDVO 98%, SVOL 80.61% (ticket), TSPY 100% (ticket) unchanged. Next five used the same process (`19.1 tax ROC (TICKER) (Provider) website data source`, vendor host only). All five already store 100% and live collect 100% — **no new `roc_pct_change` tickets** (same % does not ticket). HAKY: Amplify notices Feb–May 2026 all 100%; standing URL moved from Feb to newest posted file `19a-1_Notice_05-29-26_HAKY.pdf` (`--roc-url` only; plan 100% and last_run_ok unchanged). June–August 2026 HAKY 19a-1 PDFs are not on the Amplify files path (404; declaration press exists; unknown ≠ 0). QDTE / RDTE / TOPW / XDTE: Roundhill ETF pages, most recent 19a-1 sentence **100%**. Confirmed 19a-1 mismatches now raise `roc_pct_change` instead of stopping at probe. QYLD ticket: 80% → 99.72% August Form 19a (1099 84.10 informational); plan stays 80%. SVOL ticket: 25% → 80.61%. TSPY ticket: 85% → 100%. QDVO/AMDW still match stored % (no ticket). TSPY uses the same search → vendor hub → table/notice path. Primary is `https://tappalphafunds.com/tspy` Distributions (Amount / ROC / 19a-1 Notice), filled from the NavStar `view=all` rows. Newest posted ROC is used (future nulls skipped; `XX%` is unknown). Live probe: stored plan 85%, September 2026 table **100%** with a linked 19a-1 PDF; standing URL is the fund page. Collect must ticket 85%→100%, not overwrite. Last-year 1099 unknown. SVOL monthly S19(a) notices are now a collector path: search/vendor host, then the fund-page “S19(a) Notices” accordion (`/etfs/536/supplemental-tax-information`, 2221 fallback), newest `S19a_Notice_YYYY_Month_M.pdf` first. Parser reads the multi-fund row `(SVOL)$total$NI$ROC` (August glued ROC = total−NI). Live probe: stored plan 25%, August 2026 notice 80.61% at `…/2026-08/S19a_Notice_2026_August_M.pdf`; standing `roc_source_url` is the SVOL fund page (monthly reuse). Collect must ticket 25%→80.61%, not overwrite. ROC change tickets also show last year 1099 as informational (unknown if missing; never invented 0%). Accept/Reject still only touch the current-year projection. A later 19a-1 % that differs from the stored current-year projection no longer overwrites it. Collect raises `roc_pct_change` (`ROC % was X and now ROC % should be Y`). Accept writes the new projection; Reject leaves the previous %. First fill of an empty estimate is unchanged. Five InScope names had a stored plan ROC % but a product/hub page in `roc_source_url`. Wrote vendor 19a-1/tax reuse URLs via `roc-19a1-fill --roc-url` (last_run_ok and plan % unchanged): QYLD 80% → Global X QYLD tax-supplements index; QDVO 98% → Amplify 19a-1 PDF 05-29-26 (notice quotes 98%); AMDW 100% → Roundhill fund-filings#amdw (ETF page still cites most recent 19a-1 = 100%); SVOL 25% → Simplify S19(a) supplemental-tax index. TSPY 85%: no public vendor 19a-1 PDF that states 85% (etfs/tspy shows XX%; searches listed in chat); left product-page URL; did not invent a notice or change 85%. Trends Week income chart plots paid/declared brokerage money (same `weekIncomeMinor` as Cash Management), not stored seed Profit. Cash Management week desk and capture review keep one Week income column (paid/declared brokerage money). Seed/manual Profit is no longer shown. Live `production-disb-*` parents were missing seed federal/state withholding (ledger 0 vs template $7,672.18 / $3,096.43). Backfill + hard seed-parent identity: `data-seed` fails if those 129 rows’ gross/fed/state/net do not match the template. Later owner posts are excluded. Cash Management no longer prints unapproved lecture paragraphs (Distributions YTD, Car ROC notes, tax-section notes, tax-monitor note). Ask before adding screen summaries. Current-year Distributions YTD dropped the Taxable brokerage card. Seed `Form_1099` is 2025 ROC guidance and is excluded from this year’s YTD/week/month. Car ROC plan subtotals are remaining/YTD ordinary vs ROC plus long-term and short-term tax-lot gain or loss. 1099 actual stays parked until April 2027. Income fleet locked at 40 last-run-OK collectors (`INCOME_FLEET_SYMBOLS`). MSTU, TSLL, and SOXL are parked long-hold (`collector_enabled=false`); seed/Apply cannot re-enable them. Live Run enabled still-miss is 0. Weekly capture, FID+SCH cards, and the saved-weeks table moved from Trends to Cash Management (hub + per-activity wizards; Accept spawns follow-up). Trends is charts only. Trends removed from the Plan submenu (top-level only). Plan vs Decl week table moved to the bottom of Income Plan. Trends keeps weekly graphs. week_not_saved is not a quality error; the wizard blocks leaving until Accept. DividendPerformanceGet week Plan is the sum of names that have a plan; one unplanned name no longer blanks the week. Trends Dividend weeks lists newest first; one Week column; Plan/Decl/% blank when unknown; lecture copy removed; table spacing tightened. Auto last-price on open runs only when LastPriceAutoWindowGet returns allowed true; weekend/outside-hours skip is silent (no busy bar, no skip banner). Income Plan weekly report treats Decl $ as the week's money: columns are Plan, Decl, and Decl−Plan variance (blank until declared). Broker Actual $ and amount-exceptions stay off this screen. Current subtotals (declared names) sit above Grand Total. Auto last-price on open checks LastPriceAutoWindowGet first and does not start the last-price bar on Saturday/Sunday or outside 9–4 Eastern; manual Refresh still force-runs. Income Plan week nav loads only IncomePlanWeekGet; Weekly grid tab loads IncomePlanGridGet. Income Plan week change shows a Loading Data overlay until refresh finishes. Trends weekly capture is a wizard (week dropdown defaulting to the first unpopulated Sat–Fri week, then Income → FI Roth → Speculation → Health → Car → Account 9). Each account asks Total and Cash. Account 9 70% ETF is last-price qty, unknown if a quote is missing. Review shows system week income, Total cash, and Fidelity/Schwab week-to-week change; Accept saves through TrendsWeekSave so charts update. Health is on the Trends account charts. Home averages: previous 12 months Income on the left, Monthly Plan Income on the right (INCOME aligned; previous bold). Work Tickets button pins to the cell bottom like the other coverage actions. Home monthly averages show annual in parentheses (12 × monthly, no extra label). Home portfolio grid drops Yield events, Disbursements, and Open lots. Average Monthly Plan Income and Average previous 12 months sit side by side (Account 9, Income, FI Roth, Car only — plan annual ÷ 12; paid dividends in the prior 12 complete calendar months ÷ 12). Income Plan weekly Decl $ is the retrieved payable in that Sat–Fri week only — last week’s issuer row is not cloned onto this week’s forecast Friday (monthly leftover-ex year-month stays). Owner declaration weekdays stored on ExpectedPaymentPattern: TOPW Monday; QDTE/RDTE/XDTE and YieldMax weeklies Wednesday (holidays shift the look day, not the amount). W37 Decl $ only AMDW NVDW PLTW TSLW. 40 enabled income-fleet collector seed URLs still match their adapter hosts. Home account cards: Name, Broker, Amount on one header row (more chart height). Weekly actuals stay on Account 9 and are omitted on Speculation. Trends Risk Profile uses the shared graphing period; own period control removed; current total sits in the level-chart upper right. Shopping Cart blend is one row per position (last, symbol, %, editable qty, spend, week, month, year, annual each, yield, risk). Qty may go over budget while experimenting; Agree still blocks. Plan annual uses plan scale (HAKY 3800 at scale 4 is $0.38/sh/mo, not $38). Income columns are always week, month, year. Duplicate drafts compare blends. Account cash is the one remaining SPAXX/FDRXX/SWVXX qty. Draft does not spend it. Approve checks live cash; CashDeposit posts a ledger row and raises qty if short. Confirm fill deducts. Energy/Robinhood are not offered. Shopping Cart starts as a wizard (Account → Plan name → How funded). fundingSource persists on the draft; new deposit next screen stays parked. Shopping Cart leftover yield is the collector money-market plan (SPAXX/FDRXX/SWVXX), not a typed Cash yield (bps) field. Typed `cashYieldBps` is a fixture fallback only when no plan exists; missing plan leaves leftover/surrendered annual unknown. Home Dividend Plan sits in a fixed 580px / remainder split (Monthly Reinvest removed; Energy and Robinhood income $0; effective annual return). Account-values legend aligns to the right of Graphing period on that same split. Last-price auto refresh is weekdays 9–4 Eastern and skips when the last successful price run is under 4 hours; manual Refresh stays force. Tools → Components registry added. Graphing files moved to features/graphing/; account-value helpers to account_value.rs. Live by risk moved to the bottom of Trends: level chart and current-allocation donut side by side (level chart 50% width). Distributions YTD and Tax / ACA monitor moved from Trends to Cash Management (read-only; TrendsGet still supplies the numbers). Home Income through list filters by period and by account. Accounts count removed. Home Live by risk: Mix 100% stacked panel removed; Level unstacked lines remain. CM-4 import (parent + federal/state withholding, idempotent against the 129 seed rows). CM-5 Trends distribution/tax blocks are read-only Cash Management summaries. CM-1 prove-subtotal. CM-2 Saturday Income draft + Tom SSA $2,865 confirm. CM-3 MAGI preview (read MagiProjectionGet). CM-4 month board (event date). Home live-by-risk stacked chart + shared graphing period. Home open tickets + collector-stat truth (fleet-scoped Ran; Still miss vs Had a miss today). Weekly vendor payables merge by Sat–Fri week or 3-day window (not YYYY-MM); false weekly `paid_payable_supersede` auto-files. Live by risk: each stored last-price day reconstructed from current risk assignment (no carry-forward); double-click symbol text list. Position Details owner facts edit in the Position information table (catalog-locked). Risk is mandatory Foundation / Core / Risk On — Undecided removed from dropdowns and the Live by risk legend. Owner-edit Save turns orange while unsaved (`is-unsaved`). Home account charts: weekly actuals line (Sat–Fri, Friday plot, closed weeks only, dual Y-axis). Shopping Cart SC-1–SC-3: named-lot swap, leftover-keeps-yield, lot cost/P/L table (lowest cost or largest tax loss), named draft monthly compare. MAGI oracles unchanged.
**Parked:** M1 macOS; SQLite→Postgres cutover; live OIDC; public CA; Python connectors posting; MAGI oracle rewrite; M8/M9 theater; watchlist; full position-liquidity classification replacing Acct9 interim heuristic

`App.tsx` stays `LocalTauriFinanceClient`. No UI SQL. Never regenerate MAGI oracles.
Owner facts use in-row display/edit in the same field. Save or Cancel; leaving with unsaved edits asks first.

**Collector completion gate:** the income fleet is the 40 `INCOME_FLEET_SYMBOLS`. Issuer adapter work is not Done until `npm run desktop` → Run enabled collectors (or Run misses only until empty) shows **0 miss** and Income Plan has amounts for every enabled payer. Fixture tests alone do not satisfy this. Live lock on 13 Sep 2026: still-miss 0 on those 40. MSTU, TSLL, and SOXL are holdings only — not collectors.

## Two jobs

1. **Seed (once):** `npm run data-seed` writes `database/seed/production` into `%LOCALAPPDATA%\com.finos.desktop` (or `%LOCALAPPDATA%\finos` if that path is a sync folder). Gate: 8 / 75 / 1679 / 1250 / 5862 / 129 and open cost $466,946.66 / tax $461,356.29. Complete data is a no-op. Desktop never searches for xlsx.
2. **Daily use:** desktop opens that SQLite file and queries it. Last prices auto-refresh on open for open lots only on weekdays 9–4 Eastern (and skip if the last price retrieve_run — ok, fail, or started — is under 4 hours); a stored last price still displays when stale. Manual Refresh still force-runs. Missing last price stays unknown, never $0. Each symbol is 3s; a miss keeps the last price and the fleet continues. Income Plan weekly report uses Decl $ as the week's money (Plan, Decl, Decl−Plan); broker Actual $ is not on that screen. No load prompts. On open, ProviderDeclarationSourcesApply fills empty templates then DeclarationRefresh runs enabled collectors on that same weekday 9–4 Eastern window, once per local calendar date (skip weekend/hours, or when `declarationRefreshedOn` is already today). The 4-hour last-price cooldown does not skip collectors. Refresh declarations still force-runs. **Coding launch** this month: repo-root `finos.bat` (Restart parent window titled `finos`). It Start-Process-es `apps/desktop/start-finos-dev.bat` (`finos (dev)`). File → Restart writes `restart.token` and exits; the parent opens a new `finos (dev)` console. Do not start the child bat alone. Console is expected. **Household launch** (no console): Desktop `finos-installed.bat` → `%LOCALAPPDATA%\finos\finos-desktop.exe` after an owner-requested `npm run desktop:build`. Do not run both copies. Do not `desktop:build` on every host change.

## Miss loop (Done 13 Sep 2026)

Income fleet is locked at 40 last-run-OK names. Desktop Run enabled still-miss is 0. Residual ROC-template / Except-Reject work is a later slice.

## Pass

```
cargo test -p financial-domain
cargo test -p golden-harness --test new_investment --test add_lot --test position_details --test roc_plan
cargo test -p golden-harness --test accessibility --test invariants --test calculator_plan --test lots_roi --test dividend_slice --test production_seed --test collectors --test data_snapshot --test desktop_menu --test core_functions --test income_plan_week --test cash_management
```

Desktop stays SQLite.
